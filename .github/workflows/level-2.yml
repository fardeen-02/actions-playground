name: level-2 checkout

on: push

jobs:
  check-out:
    runs-on: ubuntu-latest
    steps:
      - name: Chekout code
        uses: actions/checkout@v4

      - name: list files
        run: ls -al
        
